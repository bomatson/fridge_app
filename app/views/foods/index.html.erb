<h1>Hello Food!</h1>

<a href='/foods/new'>Create a new food</a>
<table>
  
  <% @foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.food_type %></td>
        <td><%= food.expiration %></td>
      </tr>
  <% end %> 

</table>